[package]
name = "local_device_manager"
version = "0.1.0"
edition = "2024"
authors = [
    "Gianluca Bresolin"
]
description = "A local device manager for managing local devices in an embedded system."

[dependencies]
device_driver = { path = "../device_driver" }
simulated_led_driver = { path = "../simulated_led_driver" }