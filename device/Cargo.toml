[workspace]
members = [
    "app",
    "local_device_manager",
    "simulated_led_driver",
    "communication_component",
]
resolver = "3"

exclude = [
    "qemu"
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"