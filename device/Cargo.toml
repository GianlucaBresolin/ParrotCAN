[workspace]
members = [
    "app",
    "device_driver",
    "local_device_manager",
    "simulated_led_driver",
    "communication_component", "inter_component_exchange_manager",
]
resolver = "3"

exclude = [
    "qemu"
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
