[package]
name = "simulated_led_driver"
version = "0.1.0"
edition = "2024"
authors = [
    "Gianluca Bresolin"
]
description = "A simulated LED driver for testing purposes in an embedded system."

[dependencies]
cortex-m-semihosting = "0.5"
device_driver = { path = "../device_driver" }
